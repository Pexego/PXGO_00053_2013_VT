<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!--Email template -->
        <record id="email_template_edi_send_aviso_custom" model="mail.template">
            <field name="name">Aviso de envío</field>
            <field name="email_from">${(object.company_id.email or 'noreply@localhost')|safe}</field>
            <field name="subject">Visiotech - Productos enviados</field>
<!--            <field name="partner_to">${(object.partner_id.email2 or object.partner_id.email)|safe}</field>-->
            <field name="email_to">${(object.partner_id.email)|safe}</field>
            <field name="model_id" ref="stock.model_stock_picking"/>
            <field name="auto_delete" eval="True"/>
            <field name="report_template" ref="custom_report_link.report_picking_custom_valued_action"/>
            <field name="report_name">${(object.name or '').replace('/','_')}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="body_html"><![CDATA[
<!DOCTYPE html>

                        <p>Estimado cliente,</p>
                        % if object.carrier_name and object.carrier_name == "GENERICA":
                        <p>Le informamos de que nuestro almacén ha preparado los productos correspondientes al albarán ${object.name} del pedido ${object.sale_id.name} y le serán entregados en unos instantes.</p>
                        % elif object.carrier_name:
                        <p>Le informamos de que ya hemos preparado para envío los productos correspondientes al albarán adjunto ${object.name} del pedido ${object.sale_id.name}. Dado que este pedido está siendo recogido hoy en nuestras instalaciones por la compañía de transporte, ya no podrá realizar ninguna modificación.</p>
                        <p>La compañía de transportes responsable del envío de su pedido es ${object.carrier_name}
                            % if object.carrier_tracking_ref:
                            y el número de seguimiento de su pedido es ${object.carrier_tracking_ref}. Puede realizar el seguimiento en el siguiente enlace:</p><p>
                            % if object.carrier_name == "SEUR":
                            <a href="http://www.seur.com/livetracking/?segOnlineIdentificador=${object.carrier_tracking_ref}"> Seguimiento del pedido ${object.sale_id.name}</a>
                            % elif object.carrier_name == "CorreosExpress":
                            <a href="https://www.correosexpress.com/url/v?s=${object.carrier_tracking_ref}&amp;cp=${object.partner_id.zip}"> Seguimiento del pedido ${object.sale_id.name}</a>
                            % endif
                        </p><p>
                            <b>Tenga en cuenta que la información de seguimiento de su pedido puede no estar disponible en el momento de recepción de este e-mail. Este email se genera automáticamente, no responda a este email. Si necesita contactar con VISIOTECH puede hacerlo a través de su comercial habitual por email: <a href="mailto:${object.create_uid.login or ''}">${object.sale_id.user_id.login}</a> </b>
                            % endif
                        </p>
                        % endif
                        <p>Atentamente,</p>


]]>
            </field>
        </record>
        </data>
</odoo>