<?xml version="1.0"?>
<odoo>
    <record id="view_account_journal_form_expenses" model="ir.ui.view">
        <field name="name">account.journal.form.expenses</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <field name="group_invoice_lines" position="after">
                <field name="expenses_journal"/>
            </field>
        </field>
    </record>

    <record id="invoice_supplier_form" model="ir.ui.view">
        <field name="name">captio.invoice.supplier.form</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page/group/group/field[@name='company_id']" position="after">
                <field name="captio_img_url" widget="url" text="Imagen del gasto"/>
            </xpath>
        </field>
    </record>

</odoo>